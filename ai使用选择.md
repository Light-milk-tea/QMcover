# AI 使用选择

## 1. 核心判断

封面喵不能只靠一个生图模型完成。`FLUX.2 Pro` 这类模型只能生成一张扁平图片，不能生成可编辑的标题、图层、坐标、字体和版式。

因此，AI 能力需要拆成两类：

1. **设计编排 AI**：负责生成可编辑的画布结构，例如标题图层、副标题图层、标签图层、位置、字号、颜色、层级等。
2. **图片生成 AI**：负责生成背景图、主体图、局部重绘图等图片素材。

封面制作的重头戏是设计编排 AI，而不是单纯的出图模型。

## 2. 推荐模型

| 能力 | 推荐模型 | 选择原因 |
| --- | --- | --- |
| API 设计编排 | GPT-4o / GPT-5 系列多模态模型 | 能理解用户需求和参考图，稳定输出结构化 JSON，适合生成可编辑画布 |
| 本地设计编排 | Qwen2.5-VL | 可本地部署，支持 OCR、视觉定位和 JSON 输出，适合私有化方案 |
| API 高质量出图 | FLUX.2 Pro | 出图质量高，适合生成封面背景、主视觉、商业成品素材 |
| 本地出图 | FLUX.2 Klein 4B | 可本地部署，速度快、成本低，适合草稿图和本地预览 |
| 分割与 mask | SAM 2 | 适合主体选择、商品抠图、局部重绘区域生成 |
| 局部重绘 | GPT Image 或 FLUX 编辑 API | 适合对已有图片进行局部替换、背景调整和细节修改 |

## 3. 具体分工

### 3.1 GPT-4o / GPT-5 系列多模态模型

作为 API 侧的核心设计编排模型。它不主要负责出图，而是负责把用户输入转成可编辑的封面结构。

主要用途：

- 生成标题、副标题、标签文案。
- 生成画布 JSON。
- 决定文字位置、字号、颜色、层级。
- 根据参考图理解版式和风格。
- 给 FLUX 生成背景图 prompt。

选择原因：它的语言理解、视觉理解和结构化输出能力更适合做“封面导演”。

### 3.2 Qwen2.5-VL

作为本地部署的设计编排和视觉理解模型。

主要用途：

- 本地识别参考图中的文字和布局。
- 输出元素坐标和 bounding box。
- 生成或辅助修正画布 JSON。
- 支持私有化部署，不依赖外部 API。

选择原因：它支持本地部署，中文能力较好，并且具备 OCR、视觉定位、结构化输出能力。

### 3.3 FLUX.2 Pro

作为 API 高质量出图模型。

主要用途：

- 生成封面背景。
- 生成主视觉素材。
- 生成风格化图片。
- 生成商业成品级候选图。

选择原因：出图质感强，适合封面、海报、电商图等视觉要求高的场景。

注意：`FLUX.2 Pro` 不负责生成可编辑图层，只负责生成图片素材。

### 3.4 FLUX.2 Klein 4B

作为本地出图模型。

主要用途：

- 本地生成草稿图。
- 批量生成低成本候选图。
- 私有化部署场景下生成背景或素材。

选择原因：模型较轻，部署成本低，速度快，适合作为本地模式的默认出图模型。

### 3.5 SAM 2

作为分割和 mask 模型。

主要用途：

- 用户框选主体后生成精细 mask。
- 商品、人像、物体抠图。
- 为局部重绘提供区域范围。

选择原因：它适合做图像分割，能把用户的粗略选择变成更准确的编辑区域。

### 3.6 GPT Image 或 FLUX 编辑 API

作为局部重绘模型。

主要用途：

- 替换局部背景。
- 修改图片中的某个区域。
- 对 AI 生成图进行二次精修。

选择原因：它适合处理“只改这块区域”这类图片编辑任务。

## 4. 最终方案

封面喵首版建议采用以下组合：

```text
GPT-4o / GPT-5 系列多模态模型：生成可编辑画布 JSON
Qwen2.5-VL：本地视觉理解和本地画布结构生成
FLUX.2 Pro：API 高质量主视觉出图
FLUX.2 Klein 4B：本地草稿和预览出图
SAM 2：分割、抠图、mask
GPT Image / FLUX 编辑 API：局部重绘
```

产品实现上，不要让 FLUX 直接生成完整带字封面。更合理的方式是：**FLUX 生成背景和素材，设计编排 AI 生成可编辑图层，画布系统负责最终编辑。**
